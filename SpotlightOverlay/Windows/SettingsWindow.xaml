<Window x:Class="SpotlightOverlay.Windows.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Spotlight Overlay Settings"
        Width="420" Height="620"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E1E">
    <Window.Resources>
        <SolidColorBrush x:Key="CardBg" Color="#2D2D2D" />
        <SolidColorBrush x:Key="CardBorder" Color="#3E3E3E" />
        <SolidColorBrush x:Key="TextPrimary" Color="#E0E0E0" />
        <SolidColorBrush x:Key="TextSecondary" Color="#999999" />
        <SolidColorBrush x:Key="Accent" Color="#5B9BD5" />
        <SolidColorBrush x:Key="ControlBg" Color="#383838" />
        <SolidColorBrush x:Key="ControlHover" Color="#434343" />
        <Style x:Key="SectionLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}" />
            <Setter Property="FontSize" Value="11" />
            <Setter Property="FontWeight" Value="Medium" />
            <Setter Property="Margin" Value="0,0,0,6" />
        </Style>
        <Style x:Key="DarkThumb" TargetType="Thumb">
            <Setter Property="Width" Value="16" />
            <Setter Property="Height" Value="16" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Ellipse Fill="{StaticResource Accent}" Stroke="#1E1E1E" StrokeThickness="2" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SliderFill" TargetType="RepeatButton">
            <Setter Property="IsTabStop" Value="False" />
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border Background="{StaticResource Accent}" CornerRadius="2" Height="4" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SliderTrack" TargetType="RepeatButton">
            <Setter Property="IsTabStop" Value="False" />
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border Background="#4D4D4D" CornerRadius="2" Height="4" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="Slider">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid>
                            <Track x:Name="PART_Track">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Style="{StaticResource SliderFill}" Command="Slider.DecreaseLarge" />
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource DarkThumb}" />
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Style="{StaticResource SliderTrack}" Command="Slider.IncreaseLarge" />
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource ControlBg}" />
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
            <Setter Property="CaretBrush" Value="{StaticResource TextPrimary}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{StaticResource CardBorder}"
                                BorderThickness="1" CornerRadius="4" Padding="6,4">
                            <ScrollViewer x:Name="PART_ContentHost" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ComboToggle" TargetType="ToggleButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="Bd" Background="{StaticResource ControlBg}"
                                BorderBrush="{StaticResource CardBorder}" BorderThickness="1" CornerRadius="4">
                            <Path Data="M 0 0 L 4 4 L 8 0 Z" Fill="{StaticResource TextSecondary}"
                                  HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource ControlHover}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="DarkComboItem" TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd" Background="Transparent" Padding="{TemplateBinding Padding}">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#3E3E3E" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
            <Setter Property="Height" Value="32" />
            <Setter Property="ItemContainerStyle" Value="{StaticResource DarkComboItem}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Style="{StaticResource ComboToggle}"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          Focusable="False" ClickMode="Press" />
                            <ContentPresenter Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              IsHitTestVisible="False" Margin="10,0,28,0"
                                              VerticalAlignment="Center" HorizontalAlignment="Left" />
                            <Popup Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Grid MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}" SnapsToDevicePixels="True">
                                    <Border Background="#333333" BorderBrush="{StaticResource CardBorder}"
                                            BorderThickness="1" CornerRadius="4" Margin="0,2,0,0" Padding="0,4">
                                        <ScrollViewer SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ToggleSwitch" TargetType="ToggleButton">
            <Setter Property="Width" Value="44" />
            <Setter Property="Height" Value="22" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Grid>
                            <Border x:Name="Track" CornerRadius="11"
                                    Background="#4D4D4D" BorderThickness="0" />
                            <Border x:Name="Knob" Width="18" Height="18"
                                    CornerRadius="9" Background="#AAAAAA"
                                    HorizontalAlignment="Left" Margin="2,0,2,0"
                                    VerticalAlignment="Center" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Track" Property="Background" Value="{StaticResource Accent}" />
                                <Setter TargetName="Knob" Property="Background" Value="White" />
                                <Setter TargetName="Knob" Property="HorizontalAlignment" Value="Right" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Knob" Property="Background" Value="#CCCCCC" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Border Grid.Row="0" CornerRadius="6" ClipToBounds="True"
                Background="#2D2D2D" BorderBrush="{StaticResource CardBorder}" BorderThickness="1">
            <Canvas x:Name="PreviewArea" Height="130" Background="#E0E0E0" />
        </Border>
        <Border Grid.Row="2" Background="{StaticResource CardBg}" CornerRadius="6"
                BorderBrush="{StaticResource CardBorder}" BorderThickness="1" Padding="14,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="8" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="55" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="OVERLAY OPACITY"
                           Style="{StaticResource SectionLabel}" />
                <Slider Grid.Row="2" Grid.Column="0"
                        x:Name="OpacitySlider" Minimum="10" Maximum="90"
                        TickFrequency="10" IsSnapToTickEnabled="True"
                        LargeChange="10" SmallChange="1" VerticalAlignment="Center"
                        ValueChanged="OpacitySlider_ValueChanged" />
                <TextBox Grid.Row="2" Grid.Column="1"
                         x:Name="OpacityTextBox" VerticalAlignment="Center"
                         HorizontalAlignment="Right" Width="50" TextAlignment="Center"
                         LostFocus="OpacityTextBox_LostFocus" KeyDown="OpacityTextBox_KeyDown" />
            </Grid>
        </Border>
        <Border Grid.Row="4" Background="{StaticResource CardBg}" CornerRadius="6"
                BorderBrush="{StaticResource CardBorder}" BorderThickness="1" Padding="14,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="8" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="55" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="FEATHER RADIUS"
                           Style="{StaticResource SectionLabel}" />
                <Slider Grid.Row="2" Grid.Column="0"
                        x:Name="FeatherSlider" Minimum="0" Maximum="50"
                        TickFrequency="10" IsSnapToTickEnabled="False"
                        LargeChange="10" SmallChange="1" VerticalAlignment="Center"
                        ValueChanged="FeatherSlider_ValueChanged" />
                <TextBox Grid.Row="2" Grid.Column="1"
                         x:Name="FeatherTextBox" VerticalAlignment="Center"
                         HorizontalAlignment="Right" Width="50" TextAlignment="Center"
                         LostFocus="FeatherTextBox_LostFocus" KeyDown="FeatherTextBox_KeyDown" />
            </Grid>
        </Border>
        <Border Grid.Row="6" Background="{StaticResource CardBg}" CornerRadius="6"
                BorderBrush="{StaticResource CardBorder}" BorderThickness="1" Padding="14,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="8" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="PREVIEW STYLE" Style="{StaticResource SectionLabel}" />
                <ComboBox Grid.Row="2" x:Name="PreviewStyleCombo"
                          SelectionChanged="PreviewStyleCombo_SelectionChanged">
                    <ComboBoxItem Content="Outline" />
                    <ComboBoxItem Content="Crop Marks" />
                    <ComboBoxItem Content="Half Crop Marks (2/4 corners)" />
                </ComboBox>
            </Grid>
        </Border>
        <Border Grid.Row="8" Background="{StaticResource CardBg}" CornerRadius="6"
                BorderBrush="{StaticResource CardBorder}" BorderThickness="1" Padding="14,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="8" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="DRAG MODE" Style="{StaticResource SectionLabel}" />
                <ComboBox Grid.Row="2" x:Name="DragStyleCombo"
                          SelectionChanged="DragStyleCombo_SelectionChanged">
                    <ComboBoxItem Content="Hold and Drag" />
                    <ComboBoxItem Content="Click to Click" />
                </ComboBox>
            </Grid>
        </Border>
        <Border Grid.Row="10" Background="{StaticResource CardBg}" CornerRadius="6"
                BorderBrush="{StaticResource CardBorder}" BorderThickness="1" Padding="14,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Freeze screen while overlay is active"
                           Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center" />
                <ToggleButton Grid.Column="1" x:Name="FreezeToggle"
                              Style="{StaticResource ToggleSwitch}"
                              Checked="FreezeToggle_Changed" Unchecked="FreezeToggle_Changed" />
            </Grid>
        </Border>
    </Grid>
</Window>